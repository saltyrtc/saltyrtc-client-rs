version: 2
jobs:
  build:
    docker:
      - image: quay.io/saltyrtc/circleci-image-rs:master
    steps:
      - checkout
      # Load cargo target from cache if possible.
      # Multiple caches are used to increase the chance of a cache hit.
      - restore_cache:
          keys:
            - v5-cargo-cache-{{ .Branch }}
            - v5-cargo-cache
      - run: cargo build && cargo test
      - run: cargo audit
      - save_cache:
          key: v5-cargo-cache-{{ .Branch }}
          paths:
            - target
            - /usr/local/cargo
      - save_cache:
          key: v5-cargo-cache
          paths:
            - target
            - /usr/local/cargo
